assigning a variable to a defined function to make it work
e.g. n = int count_apple(void)

global variables
const int N = 3;

arrays
int scores[3];
scores[0] = 1;
scores[1] = 2;
scores[3] = 3;


typedef struct
{
    string name;
    string number;
}
person;

person people[4];
people[0].name = "EMMA";
people[0].number = "215623625";

to round up int
{
    float a = 5;
    float b = 4;
    float c = 5;
    int d = round((a + b + c) / 3)
}
